import { fromJS } from 'immutable';

export default fromJS({
  page: {
    page: "login",
    pageTitle: "Login",
    drawerOpen: false,
    snackbar: {
      open: false,
      message: "",
    },
  },
  groceryLists: {
  },
});
