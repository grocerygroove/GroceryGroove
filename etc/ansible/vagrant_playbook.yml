- name: provision development vagrant
  hosts: all
  remote_user: "{{ deploy_user }}"
  become: yes
  become_method: sudo
  roles:
    - ppa_postgres
    - common
    - nginx
    - node
    - postgres
    - rambler
    - gg_db
    - gg_api
